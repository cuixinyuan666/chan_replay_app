name: chan_replay_app
description: 缠论 K 线复盘 MVP，支持本地 CSV、逐 K 回放、分型、笔、中枢。
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  file_picker: ^8.1.7
  http: ^1.2.2
  candlesticks: ^3.0.1
  url_launcher: ^6.3.1
  path_provider: ^2.1.5
  window_manager: ^0.4.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/sample_data/000001_daily.csv
